<style>
:root{
  --bg:#F5F7F9; --surface:#FFFFFF; --ink:#172029; --ink-2:#4B5966; --line:#D6DDE4; --line-2:#E9EEF2;
  --accent:#1E5F7C; --accent-ink:#FFFFFF; --accent-soft:#E3EEF3;
  --warn:#B4581B; --warn-soft:#F8EAD9; --crit:#9C2A2A; --crit-soft:#F6E1E1; --ok:#2E6B45; --ok-soft:#E1EFE5;
  --code-bg:#EEF2F5;
}
@media (prefers-color-scheme: dark){
  :root:not([data-theme="light"]){
    --bg:#111820; --surface:#18212B; --ink:#E6ECF1; --ink-2:#A2AFBB; --line:#2C3946; --line-2:#22303C;
    --accent:#6FB3D0; --accent-ink:#0E1A22; --accent-soft:#1C2F3A;
    --warn:#E6924E; --warn-soft:#3A2A1A; --crit:#E07070; --crit-soft:#3B1F1F; --ok:#7CC496; --ok-soft:#1C3324;
    --code-bg:#0F161D;
  }
}
:root[data-theme="dark"]{
  --bg:#111820; --surface:#18212B; --ink:#E6ECF1; --ink-2:#A2AFBB; --line:#2C3946; --line-2:#22303C;
  --accent:#6FB3D0; --accent-ink:#0E1A22; --accent-soft:#1C2F3A;
  --warn:#E6924E; --warn-soft:#3A2A1A; --crit:#E07070; --crit-soft:#3B1F1F; --ok:#7CC496; --ok-soft:#1C3324;
  --code-bg:#0F161D;
}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--ink);font-family:"IBM Plex Sans",system-ui,-apple-system,"Segoe UI",sans-serif;font-size:16px;line-height:1.55;-webkit-font-smoothing:antialiased}
a{color:var(--accent)}
.wrap{max-width:1140px;margin:0 auto;padding:40px 24px 96px;display:grid;grid-template-columns:200px minmax(0,1fr);gap:48px}
@media (max-width:860px){.wrap{grid-template-columns:minmax(0,1fr);gap:24px}}
nav.toc{position:sticky;top:24px;align-self:start;font-size:13px}
@media (max-width:860px){nav.toc{position:static}}
nav.toc .eyebrow{margin-bottom:10px}
nav.toc a{display:block;color:var(--ink-2);text-decoration:none;padding:5px 0;border-left:2px solid var(--line-2);padding-left:10px}
nav.toc a:hover{color:var(--ink);border-color:var(--accent)}
main{max-width:72ch}
.eyebrow{font-family:"IBM Plex Mono",ui-monospace,monospace;font-size:11.5px;letter-spacing:.12em;text-transform:uppercase;color:var(--ink-2)}
h1{font-family:Archivo,"IBM Plex Sans",sans-serif;font-weight:800;font-size:44px;line-height:1.05;letter-spacing:-.02em;margin:8px 0 12px;text-wrap:balance}
h2{font-family:Archivo,"IBM Plex Sans",sans-serif;font-weight:700;font-size:26px;line-height:1.15;letter-spacing:-.01em;margin:64px 0 14px;padding-top:20px;border-top:1px solid var(--line);text-wrap:balance}
h3{font-family:Archivo,"IBM Plex Sans",sans-serif;font-weight:700;font-size:18px;margin:32px 0 8px;text-wrap:balance}
p{margin:0 0 14px}
.lede{font-size:19px;color:var(--ink-2);line-height:1.45;margin-bottom:28px}
.meta{display:flex;flex-wrap:wrap;gap:8px 20px;font-family:"IBM Plex Mono",monospace;font-size:12.5px;color:var(--ink-2);margin-bottom:36px}
.kv{display:grid;grid-template-columns:max-content 1fr;gap:6px 18px;margin:12px 0 18px;font-size:15px}
.kv dt{color:var(--ink-2);font-family:"IBM Plex Mono",monospace;font-size:12.5px;padding-top:3px}
.kv dd{margin:0}
.callout{border-left:3px solid var(--accent);background:var(--accent-soft);padding:14px 18px;margin:18px 0;border-radius:0 6px 6px 0}
.callout.warn{border-color:var(--warn);background:var(--warn-soft)}
.callout.crit{border-color:var(--crit);background:var(--crit-soft)}
.callout p:last-child{margin:0}
.tablewrap{overflow-x:auto;margin:14px 0 22px;border:1px solid var(--line);border-radius:6px;background:var(--surface)}
table{border-collapse:collapse;width:100%;font-size:14px;min-width:560px}
th,td{padding:9px 12px;text-align:left;vertical-align:top;border-bottom:1px solid var(--line-2)}
th{font-family:"IBM Plex Mono",monospace;font-size:11.5px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-2);font-weight:500;background:var(--bg)}
tr:last-child td{border-bottom:0}
td.num{font-family:"IBM Plex Mono",monospace;font-variant-numeric:tabular-nums;white-space:nowrap}
code,pre{font-family:"IBM Plex Mono",ui-monospace,monospace}
code{font-size:.9em;background:var(--code-bg);padding:1px 5px;border-radius:3px}
pre{background:var(--code-bg);border:1px solid var(--line);border-radius:6px;padding:14px 16px;overflow-x:auto;font-size:13px;line-height:1.5;margin:12px 0 20px}
pre code{background:none;padding:0}
.pill{display:inline-block;font-family:"IBM Plex Mono",monospace;font-size:11px;letter-spacing:.06em;text-transform:uppercase;padding:2px 8px;border-radius:999px;border:1px solid var(--line);color:var(--ink-2);white-space:nowrap}
.pill.crit{color:var(--crit);border-color:var(--crit);background:var(--crit-soft)}
.pill.warn{color:var(--warn);border-color:var(--warn);background:var(--warn-soft)}
.pill.ok{color:var(--ok);border-color:var(--ok);background:var(--ok-soft)}
.pill.acc{color:var(--accent);border-color:var(--accent);background:var(--accent-soft)}
ul,ol{padding-left:22px;margin:0 0 14px}
li{margin-bottom:6px}
.surfaces{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin:16px 0 8px}
@media (max-width:640px){.surfaces{grid-template-columns:minmax(0,1fr)}}
.surface{background:var(--surface);border:1px solid var(--line);border-radius:6px;padding:16px 18px}
.surface h4{font-family:Archivo,sans-serif;font-size:16px;margin:0 0 4px}
.surface .who{font-size:13px;color:var(--ink-2);margin-bottom:10px}
.surface ul{font-size:14px;padding-left:18px;margin:0}
.diagram{background:var(--surface);border:1px solid var(--line);border-radius:6px;padding:12px;margin:14px 0 22px;overflow-x:auto}
.diagram svg{display:block;min-width:640px;width:100%;height:auto}
.stages{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:6px;margin:14px 0 20px}
@media (max-width:760px){.stages{grid-template-columns:repeat(3,minmax(0,1fr))}}
.stage{background:var(--surface);border:1px solid var(--line);border-radius:5px;padding:10px;font-size:12.5px;line-height:1.35}
.stage b{display:block;font-family:Archivo,sans-serif;font-size:13px;margin-bottom:4px}
.stage span{color:var(--ink-2)}
.src{font-size:13.5px;color:var(--ink-2)}
.src li{margin-bottom:4px}
small.fn{color:var(--ink-2);font-size:13px}
</style>