{
  "name": "@guardian/integration",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@guardian/schema": "workspace:*",
    "@guardian/audit": "workspace:*",
    "@guardian/ingest": "workspace:*",
    "@guardian/scorer": "workspace:*",
    "ioredis": "^5.4.0"
  }
}
