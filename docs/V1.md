# Version 1

One definition, one list, one order. `ROADMAP.md` records what happened;
this page says what is left and when it is finished. If a "what next"
answer disagrees with this page, this page is wrong and needs editing,
not the answer.

## What version 1 is

**One Discord server, protected end to end.**

The loop from the product statement, working for real on somebody's
server: messages arrive, Guardian scores them for the six-stage ladder,
anything that looks like the pattern lands in a review queue, a person
confirms it, and a confirmed case becomes a CyberTipline report with a
tamper-evident evidence bundle and a one-year preservation hold.

That is a complete product. It is the first of the four surfaces, and
DESIGN.md section 11 puts it first for the same reason: it is the only
one that needs nobody's signature to start.

## What version 1 is not, and why

| Excluded | Why |
|---|---|
| Investigator triage (phase 4) | Cannot start without a signed LE or NGO partner naming a custodian and scope. No amount of code moves it. |
| Parent app (phase 5) | Counsel has to clear the consent posture first, and the vendor exposure is 18 USC 2512 and FTC stalkerware precedent. DESIGN.md section 11 puts it last. |
| Stage classifier, learned fusion | Both need PANC or PJZC, which are decoy transcripts Guardian does not hold. The rule kernel is the product until then, and the model card says so. |
| Voice and video (S5) | Needs a partner model. Declared in the DESIGN.md section 5 catalog at weight "none" so it reads as a known false negative rather than an absence. |
| Platform SDK go-live | Built and tested. It ships when a customer exists; nothing is left to write. |

The platform SDK is the one row where "not in v1" means "waiting for a
buyer" rather than "waiting for work".

## The four gates

Version 1 is done when all four pass.

| Gate | Test |
|---|---|
| **G1. It runs** | The bot is in a real guild, scoring real traffic, and the mod channel has received an alert nobody wrote by hand. |
| **G2. A person can finish a case** | A reviewer proposes T3 in the console and a second reviewer upholds it in the console. No script, no test harness. |
| **G3. The report is real** | A confirmed T3 produces a CyberTipline submission NCMEC accepts, or a drafted bundle the operator files, with the one-year hold set. |
| **G4. Nothing on screen breaks a rule** | Every string passes the accusation guard, the byte guard holds, retention runs, and the audit chain verifies offline. |

G2 and G4 pass. G1 and G3 do not, and both are waiting on somebody
outside this repository.

## The work, in order

### 1. The T3 path in the console

**Done.**

`recordDecision` accepted a `Concurrence` and refused T3 without one, and
nothing in the UI ever supplied one: `scripts/integration/e2e.test.ts`
was the only thing in the repository that had ever produced a T3. A
proposal left the pair at T2 with `resolvedAt` unwritten, so it returned
to the queue looking exactly like every other T2, and there was no
control to uphold it, overturn it, or withdraw it. The product
statement's fourth sentence, "a person confirms it", was not reachable by
a person.

| | Work | |
|---|---|---|
| 1.1 | `QueueCase` and `CaseDetail` carry the open proposal: who proposed, when, and the reason. The queue row says so, and it is the only thing that reorders the queue ahead of tier. | **done** |
| 1.2 | A concurrence panel on the case for a reviewer who is not the proposer: uphold, overturn, each with a reason, wired to the `concurrence` argument `recordDecision` already takes. Overturn returns the pair to T2 and records why. | **done** |
| 1.3 | A withdraw control for the proposer, live until somebody decides. The console already promised this in two strings and offered it nowhere. | **done** |
| 1.4 | The one-seat branch. A customer with one reviewer can never produce T3, and the settings page already said so where nobody deciding a case would read it. | **done**, D-3 answered below |
| 1.5 | Delete or build the two permanently disabled buttons in the decision panel. Escalate is 1.2 under another name. Request context needs an operator message path that does not exist. | **done**, both deleted |

**What the build turned up on the way.** `Review` had no column for the
state of a decision and none for the proposal a concurrence answers, so
an open proposal was not answerable from a query at all; both are columns
now, and the state column is also the lock, because closing the proposal
is an `updateMany` with `state: "proposed"` in the where clause and a
second answer matches no row.

And the read gate was satisfied by the wrong person. `Pair.humanViewedAt`
is set by whoever read first and records nobody, so the second reviewer
passed it on the proposer's read: they could have written T3 having
opened nothing. The check is now against that reviewer's own
`evidence.read` entries on the chain, which is the only place the reader
is named. That also meant `markExcerptsViewed` had to stop returning
early when every flag was already set, because a second reader wrote
nothing, returned nothing and appended nothing.

Ten unit tests, eight component tests, and the live-Postgres e2e now
covers the proposal being closed by the answer that names it.

### 2. Run the bot

Nothing here is a Guardian code change.

| | Work | Who |
|---|---|---|
| 2.1 | Create a Discord application, toggle Message Content on, put the token in `DISCORD_BOT_TOKEN` and the id in `DISCORD_APP_ID`. Under the privileged-intent review threshold this needs no approval. | Will |
| 2.2 | `pnpm --filter @guardian/discord-bot run register` then run the bot against a guild you own. `/guardian setup` picks the mod channel, `/guardian role` maps roles to bands. | Will, 30 min |
| 2.3 | Watch it for a week. The base-rate simulation currently runs on generated traffic and the model card says so; this is the first real number. | Will |
| 2.4 | Whatever week one breaks. | me |

Only after a guild that is not yours: member disclosure text, and the operator terms clause forbidding redistribution of the mod-channel embed. Both are counsel-adjacent and neither blocks your own server.

### 3. Close the open sweep findings

All code, none blocked, in the order they matter. S-2 is done.

| | Finding | Note |
|---|---|---|
| ~~**S-2**~~ | Actor band provenance and confidence were never written. | **done.** `ActorState` carries both, the kernel applies the event's reading on both sides of the pair, and `resolveBandReading` orders the seven sources so a weaker one cannot overwrite a stronger one and the band moves with its provenance. The pair's cached bands take the same resolved values, so the score and the case page cannot disagree about which band is in force. |
| **S-9** | `modelTier` on a review row is documented as the tier the model assigned and is set from `pair.tier` at open time, which can be a reviewer's T3. | Migration. Small, and it should land with section 1 because concurrence writes this column. |
| **S-6** | A scoped audit export leaks a per-version census of the withheld rows. | Rule 8. Removing it costs a reader the ability to tell a redacted export from a short one; say so in the artifact instead. |
| ~~**S-4**~~ | Raw message text sat in Redis for as long as the stream was retained. | **done. Rule 7 covers the queue.** A queued event is raw text Guardian is holding, and which store it sits in does not change what it is, so the sweep gained a `streams` step at the same 24 hour cutoff rather than the rule gaining an exception. The MAXLEN on append was never a retention control: Redis keeps an entry after XACK, so a quiet partition held every message it had ever carried. |

### 4. Make the report real

| | Work | Blocked on |
|---|---|---|
| 4.1 | NCMEC ESP registration | NCMEC. The client, the credential store and the completeness scorer are built and tested. |
| 4.2 | P-1: confirm whether a hash-only `fileDetails` record is accepted | NCMEC, at registration. If the answer is no, the operator uploads from their own systems and Guardian's report names files by hash. Rule 1 does not bend. |
| 4.3 | Until 4.1: the drafted bundle is the path | Already built. It prints both accounts neutrally, names neither, and blocks filing until a reviewer designates the subject. |

G3 passes on the drafted bundle. The ESP submission is the upgrade, not the gate.

### 5. Consistency work

Small, and each one is a thing on screen that says something untrue.

| | Work |
|---|---|
| 5.1 | `/queue` and `/cases` are the same ranked list under two nav entries with two different row designs. Pick one. |
| 5.2 | `apps/review/README.md` still says the reviewer console is "not built yet". |
| 5.3 | `docs/DESIGN-UI.md` sections 5.7 `/shift` and 10 still describe the session budget, which is gone. Section 11 is marked as a design note; those two are not. |

## Decisions you owe before the code can finish

| | Decision | Blocks |
|---|---|---|
| ~~**D-3**~~ | **Answered: nobody. A one-seat partition ends at the drafted bundle.** The operator files it themselves on the CyberTipline public form. The alternatives were Guardian staffing the second reviewer, which changes who is reporting and needs counsel, and an NGO pool, which needs the partner phase 4 is already waiting on. Neither is worth blocking v1 for. `DecisionPanel` refuses the proposal on a one-seat partition and says the bundle is the path, and the filing card says the same thing. | nothing now |
| ~~**S-4**~~ | ~~Does rule 7 cover the Redis queue~~ **Answered: yes.** The words are the words; the store they sit in does not change what Guardian is holding. | nothing now |
| **D-1** | Build the reviewer queue or adopt ROOST Coop | Nothing now. The queue is built. Answer it so it stops being asked. |
| **D-2** | Scope to the stranger and financial pattern, or build the known-contact path | Not v1 either way, but the docs currently say nothing, and silence reads as a claim Guardian does not support. |

## Order

1. ~~Section 1, the concurrence path.~~ Done.
2. ~~S-2.~~ Done, before real traffic, so the first cases carry band evidence.
3. Sections 2.1 to 2.3, your half.
4. S-9. Concurrence writes that column, so it is next.
5. Section 5, then S-6.
6. NCMEC registration whenever you are ready to start it. It is a queue with a wait, so starting it early costs nothing.

Sections 1, 3 and 5 are mine and need nothing from anybody. Section 2 is
yours. Section 4 is a third party's.
